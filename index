<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bloom Salon - Dashboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Dancing+Script:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        :root {
            --primary-color: #c39086;
            --primary-dark: #a67a70;
            --primary-light: #e2bdb5;
            --secondary-color: #d4a59a;
            --accent-color: #f4e4e1;
            --text-color: #5a4a47;
            --text-light: #fff;
            --confirmed-color: #81c784;
            --pending-color: #ffb74d;
            --cancelled-color: #e57373;
            --border-color: #e8d5d1;
            --sidebar-bg: linear-gradient(135deg, #c39086 0%, #a67a70 100%);
            --card-shadow: 0 8px 32px rgba(195, 144, 134, 0.15);
            --hover-shadow: 0 12px 40px rgba(195, 144, 134, 0.25);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(135deg, #faf8f7 0%, #f4e4e1 100%);
            color: var(--text-color);
            font-family: 'Poppins', sans-serif;
            overflow-x: hidden;
        }

        .container {
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar */
        .sidebar {
            width: 280px;
            background: var(--sidebar-bg);
            color: var(--text-light);
            padding: 0;
            display: flex;
            flex-direction: column;
            box-shadow: 4px 0 20px rgba(0, 0, 0, 0.1);
            position: relative;
            overflow: hidden;
        }

        .sidebar::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="petals" x="0" y="0" width="20" height="20" patternUnits="userSpaceOnUse"><circle cx="10" cy="10" r="1" fill="rgba(255,255,255,0.1)"/></pattern></defs><rect width="100" height="100" fill="url(%23petals)"/></svg>');
            opacity: 0.3;
        }

        .logo-container {
            padding: 30px 20px;
            text-align: center;
            position: relative;
            z-index: 2;
        }

        .logo-text {
            font-family: 'Dancing Script', cursive;
            font-size: 28px;
            font-weight: 600;
            margin-bottom: 5px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
        }

        .logo-subtitle {
            font-size: 12px;
            opacity: 0.9;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .menu-item {
            padding: 18px 25px;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            z-index: 2;
            border-left: 4px solid transparent;
            font-weight: 500;
        }

        .menu-item:hover {
            background: rgba(255, 255, 255, 0.15);
            border-left-color: rgba(255, 255, 255, 0.8);
            transform: translateX(5px);
        }

        .menu-item.active {
            background: rgba(255, 255, 255, 0.2);
            border-left-color: var(--text-light);
            transform: translateX(5px);
        }

        /* Main Content */
        .main-content {
            flex: 1;
            padding: 30px;
            overflow-y: auto;
            background: transparent;
        }

        .page-section {
            display: none;
            animation: fadeInUp 0.6s ease-out;
        }

        .page-section.active {
            display: block;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .header {
            margin-bottom: 30px;
        }

        .page-title {
            font-family: 'Dancing Script', cursive;
            font-size: 36px;
            font-weight: 600;
            color: var(--primary-dark);
            margin-bottom: 10px;
        }

        .page-subtitle {
            color: var(--text-color);
            opacity: 0.8;
            font-size: 16px;
        }

        /* Dashboard Cards */
        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
            margin-bottom: 30px;
        }

        .card {
            background: white;
            border-radius: 20px;
            padding: 25px;
            box-shadow: var(--card-shadow);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border: 1px solid var(--border-color);
            position: relative;
            overflow: hidden;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: var(--hover-shadow);
        }

        .card-title {
            font-size: 18px;
            font-weight: 600;
            color: var(--primary-dark);
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .metric-card {
            text-align: center;
        }

        .metric-value {
            font-size: 36px;
            font-weight: 700;
            color: var(--primary-color);
            margin-bottom: 5px;
        }

        .metric-label {
            font-size: 14px;
            color: var(--text-color);
            opacity: 0.8;
        }

        .metric-change {
            font-size: 12px;
            margin-top: 8px;
            padding: 4px 8px;
            border-radius: 12px;
            font-weight: 500;
        }

        .metric-change.positive {
            background: rgba(129, 199, 132, 0.2);
            color: #4caf50;
        }

        .metric-change.negative {
            background: rgba(229, 115, 115, 0.2);
            color: #f44336;
        }

        /* Chart Container */
        .chart-container {
            position: relative;
            height: 300px;
            margin-top: 20px;
        }

        .chart-small {
            height: 200px;
        }

        /* Services Section */
        .services {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .service-card {
            background: white;
            border-radius: 16px;
            padding: 20px;
            text-align: center;
            box-shadow: var(--card-shadow);
            transition: all 0.3s ease;
            border: 1px solid var(--border-color);
        }

        .service-card:hover {
            transform: translateY(-3px);
            box-shadow: var(--hover-shadow);
        }

        .service-name {
            color: var(--primary-dark);
            font-weight: 600;
            margin-bottom: 10px;
            font-size: 16px;
            text-transform: capitalize;
        }

        .service-count {
            font-size: 28px;
            font-weight: 700;
            color: var(--primary-color);
        }

        /* Schedule Grid */
        .schedule-container {
            background: white;
            border-radius: 20px;
            padding: 25px;
            box-shadow: var(--card-shadow);
            border: 1px solid var(--border-color);
            overflow-x: auto;
        }

        .schedule-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .schedule-title {
            font-size: 20px;
            font-weight: 600;
            color: var(--primary-dark);
        }

        .date-display {
            font-size: 16px;
            color: var(--text-color);
            opacity: 0.8;
        }

        .schedule-grid-container {
            display: flex;
            min-width: 800px;
        }

        .time-column {
            width: 80px;
            border-right: 1px solid var(--border-color);
        }

        .time-slot {
            height: 60px;
            padding: 5px;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            font-weight: 500;
            color: var(--text-color);
        }

        .schedule-grid {
            display: flex;
            flex: 1;
        }

        .service-column {
            flex: 1;
            border-right: 1px solid var(--border-color);
            min-width: 150px;
        }

        .service-column:last-child {
            border-right: none;
        }

        .service-column-header {
            height: 50px;
            background: var(--accent-color);
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            color: var(--primary-dark);
            text-transform: capitalize;
            border-bottom: 2px solid var(--primary-light);
        }

        .appointment-slot {
            height: 60px;
            border-bottom: 1px solid var(--border-color);
            padding: 5px;
            position: relative;
            cursor: pointer;
            transition: background-color 0.2s ease;
        }

        .appointment-slot:hover {
            background-color: rgba(195, 144, 134, 0.05);
        }

        .appointment {
            position: absolute;
            left: 5px;
            right: 5px;
            top: 5px;
            bottom: 5px;
            padding: 8px;
            border-radius: 8px;
            font-size: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .appointment:hover {
            transform: scale(1.02);
        }

        .appointment.confirmed {
            background: linear-gradient(135deg, rgba(129, 199, 132, 0.2), rgba(129, 199, 132, 0.3));
            border: 1px solid var(--confirmed-color);
        }

        .appointment.pending {
            background: linear-gradient(135deg, rgba(255, 183, 77, 0.2), rgba(255, 183, 77, 0.3));
            border: 1px solid var(--pending-color);
        }

        .appointment.cancelled {
            background: linear-gradient(135deg, rgba(229, 115, 115, 0.2), rgba(229, 115, 115, 0.3));
            border: 1px solid var(--cancelled-color);
        }

        .appointment-name {
            font-weight: 600;
            margin-bottom: 2px;
        }

        .appointment-status {
            font-size: 10px;
            opacity: 0.8;
        }

        /* Modal */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 1000;
            justify-content: center;
            align-items: center;
            backdrop-filter: blur(5px);
        }

        .modal-content {
            background: white;
            padding: 30px;
            border-radius: 20px;
            width: 500px;
            max-width: 90%;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            animation: modalSlideIn 0.3s ease-out;
        }

        @keyframes modalSlideIn {
            from {
                opacity: 0;
                transform: translateY(-50px) scale(0.9);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 1px solid var(--border-color);
        }

        .modal-title {
            font-size: 22px;
            font-weight: 600;
            color: var(--primary-dark);
            font-family: 'Dancing Script', cursive;
        }

        .close-btn {
            cursor: pointer;
            font-size: 24px;
            color: var(--text-color);
            transition: color 0.2s ease;
        }

        .close-btn:hover {
            color: var(--primary-color);
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: var(--text-color);
        }

        .form-group input, .form-group select, .form-group textarea {
            width: 100%;
            padding: 12px 16px;
            border: 2px solid var(--border-color);
            border-radius: 12px;
            font-size: 14px;
            transition: all 0.3s ease;
            font-family: 'Poppins', sans-serif;
        }

        .form-group input:focus, .form-group select:focus, .form-group textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(195, 144, 134, 0.1);
        }

        .form-group textarea {
            resize: vertical;
            min-height: 80px;
        }

        .btn {
            padding: 12px 24px;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            font-weight: 500;
            font-size: 14px;
            transition: all 0.3s ease;
            font-family: 'Poppins', sans-serif;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(195, 144, 134, 0.4);
        }

        .btn-success {
            background: linear-gradient(135deg, #81c784, #66bb6a);
            color: white;
        }

        .btn-success:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(129, 199, 132, 0.4);
        }

        .btn-danger {
            background: linear-gradient(135deg, #e57373, #ef5350);
            color: white;
        }

        .btn-danger:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(229, 115, 115, 0.4);
        }

        .btn-warning {
            background: linear-gradient(135deg, #ffb74d, #ffa726);
            color: white;
        }

        .btn-warning:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(255, 183, 77, 0.4);
        }

        .modal-footer {
            display: flex;
            justify-content: flex-end;
            gap: 15px;
            margin-top: 25px;
            padding-top: 20px;
            border-top: 1px solid var(--border-color);
        }

        .appointment-actions {
            display: flex;
            gap: 10px;
            margin-top: 15px;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .sidebar {
                width: 250px;
            }
            
            .main-content {
                padding: 20px;
            }
            
            .dashboard-grid {
                grid-template-columns: 1fr;
            }
            
            .services {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        /* Loading Animation */
        .loading {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            border-top-color: white;
            animation: spin 1s ease-in-out infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Sidebar -->
        <div class="sidebar">
            <div class="logo-container">
                <div class="logo-text">Bloom Salon</div>
                <div class="logo-subtitle">by Nicole Oliveira</div>
            </div>
            <div class="menu-item active" data-page="dashboard">Painel Gerencial</div>
            <div class="menu-item" data-page="agenda">Agenda</div>
        </div>

        <!-- Main Content -->
        <div class="main-content">
            <!-- Dashboard Section -->
            <div class="page-section active" id="dashboard">
                <div class="header">
                    <h1 class="page-title">Painel Gerencial</h1>
                    <p class="page-subtitle">Visão geral do seu negócio</p>
                </div>

                <!-- Metrics Cards -->
                <div class="dashboard-grid">
                    <div class="card metric-card">
                        <div class="card-title">Agendamentos Hoje</div>
                        <div class="metric-value">12</div>
                        <div class="metric-label">agendamentos</div>
                        <div class="metric-change positive">+15% vs ontem</div>
                    </div>
                    
                    <div class="card metric-card">
                        <div class="card-title">Receita Semanal</div>
                        <div class="metric-value">R$ 2.850</div>
                        <div class="metric-label">esta semana</div>
                        <div class="metric-change positive">+8% vs semana anterior</div>
                    </div>
                    
                    <div class="card metric-card">
                        <div class="card-title">Taxa de Ocupação</div>
                        <div class="metric-value">78%</div>
                        <div class="metric-label">dos horários</div>
                        <div class="metric-change positive">+5% vs semana anterior</div>
                    </div>
                    
                    <div class="card metric-card">
                        <div class="card-title">Novos Clientes</div>
                        <div class="metric-value">8</div>
                        <div class="metric-label">este mês</div>
                        <div class="metric-change positive">+25% vs mês anterior</div>
                    </div>
                </div>

                <!-- Charts Row -->
                <div class="dashboard-grid">
                    <div class="card">
                        <div class="card-title">Agendamentos por Serviço</div>
                        <div class="chart-container chart-small">
                            <canvas id="servicesChart"></canvas>
                        </div>
                    </div>
                    
                    <div class="card">
                        <div class="card-title">Receita Semanal</div>
                        <div class="chart-container chart-small">
                            <canvas id="revenueChart"></canvas>
                        </div>
                    </div>
                </div>

                <!-- Social Media Engagement -->
                <div class="dashboard-grid">
                    <div class="card">
                        <div class="card-title">Engajamento Instagram</div>
                        <div class="chart-container">
                            <canvas id="instagramChart"></canvas>
                        </div>
                    </div>
                    
                    <div class="card">
                        <div class="card-title">Comparativo Semanal - Redes Sociais</div>
                        <div class="chart-container">
                            <canvas id="socialComparisonChart"></canvas>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Agenda Section -->
            <div class="page-section" id="agenda">
                <div class="header">
                    <h1 class="page-title">Agenda</h1>
                    <p class="page-subtitle">Gerencie seus agendamentos</p>
                </div>

                <!-- Services Summary -->
                <div class="services">
                    <div class="service-card">
                        <div class="service-name">maquiagem</div>
                        <div class="service-count" id="maquiagem-count">5</div>
                    </div>
                    <div class="service-card">
                        <div class="service-name">unhas</div>
                        <div class="service-count" id="unhas-count">5</div>
                    </div>
                    <div class="service-card">
                        <div class="service-name">cabelos</div>
                        <div class="service-count" id="cabelos-count">5</div>
                    </div>
                    <div class="service-card">
                        <div class="service-name">noivas</div>
                        <div class="service-count" id="noivas-count">2</div>
                    </div>
                </div>

                <!-- Schedule Grid -->
                <div class="schedule-container">
                    <div class="schedule-header">
                        <div class="schedule-title">Agenda do Dia</div>
                        <div class="date-display" id="current-date"></div>
                    </div>
                    
                    <div class="schedule-grid-container">
                        <div class="time-column">
                            <div class="service-column-header">Horário</div>
                            <div class="time-slot">08:00</div>
                            <div class="time-slot">08:30</div>
                            <div class="time-slot">09:00</div>
                            <div class="time-slot">09:30</div>
                            <div class="time-slot">10:00</div>
                            <div class="time-slot">10:30</div>
                            <div class="time-slot">11:00</div>
                            <div class="time-slot">11:30</div>
                            <div class="time-slot">12:00</div>
                            <div class="time-slot">12:30</div>
                            <div class="time-slot">13:00</div>
                            <div class="time-slot">13:30</div>
                            <div class="time-slot">14:00</div>
                            <div class="time-slot">14:30</div>
                            <div class="time-slot">15:00</div>
                            <div class="time-slot">15:30</div>
                            <div class="time-slot">16:00</div>
                            <div class="time-slot">16:30</div>
                            <div class="time-slot">17:00</div>
                            <div class="time-slot">17:30</div>
                            <div class="time-slot">18:00</div>
                        </div>

                        <div class="schedule-grid">
                            <!-- Maquiagem Column -->
                            <div class="service-column" data-service="maquiagem">
                                <div class="service-column-header">Maquiagem</div>
                                <div class="appointment-slot" data-time="08:00"></div>
                                <div class="appointment-slot" data-time="08:30"></div>
                                <div class="appointment-slot" data-time="09:00">
                                    <div class="appointment confirmed" data-id="1">
                                        <div class="appointment-name">Maria Silva</div>
                                        <div class="appointment-status">confirmado</div>
                                    </div>
                                </div>
                                <div class="appointment-slot" data-time="09:30"></div>
                                <div class="appointment-slot" data-time="10:00"></div>
                                <div class="appointment-slot" data-time="10:30"></div>
                                <div class="appointment-slot" data-time="11:00"></div>
                                <div class="appointment-slot" data-time="11:30"></div>
                                <div class="appointment-slot" data-time="12:00"></div>
                                <div class="appointment-slot" data-time="12:30"></div>
                                <div class="appointment-slot" data-time="13:00"></div>
                                <div class="appointment-slot" data-time="13:30"></div>
                                <div class="appointment-slot" data-time="14:00"></div>
                                <div class="appointment-slot" data-time="14:30"></div>
                                <div class="appointment-slot" data-time="15:00"></div>
                                <div class="appointment-slot" data-time="15:30"></div>
                                <div class="appointment-slot" data-time="16:00"></div>
                                <div class="appointment-slot" data-time="16:30"></div>
                                <div class="appointment-slot" data-time="17:00"></div>
                                <div class="appointment-slot" data-time="17:30"></div>
                                <div class="appointment-slot" data-time="18:00"></div>
                            </div>

                            <!-- Unhas Column -->
                            <div class="service-column" data-service="unhas">
                                <div class="service-column-header">Unhas</div>
                                <div class="appointment-slot" data-time="08:00"></div>
                                <div class="appointment-slot" data-time="08:30"></div>
                                <div class="appointment-slot" data-time="09:00"></div>
                                <div class="appointment-slot" data-time="09:30"></div>
                                <div class="appointment-slot" data-time="10:00"></div>
                                <div class="appointment-slot" data-time="10:30"></div>
                                <div class="appointment-slot" data-time="11:00">
                                    <div class="appointment pending" data-id="2">
                                        <div class="appointment-name">Joana Santos</div>
                                        <div class="appointment-status">pendente</div>
                                    </div>
                                </div>
                                <div class="appointment-slot" data-time="11:30"></div>
                                <div class="appointment-slot" data-time="12:00"></div>
                                <div class="appointment-slot" data-time="12:30"></div>
                                <div class="appointment-slot" data-time="13:00"></div>
                                <div class="appointment-slot" data-time="13:30"></div>
                                <div class="appointment-slot" data-time="14:00"></div>
                                <div class="appointment-slot" data-time="14:30"></div>
                                <div class="appointment-slot" data-time="15:00"></div>
                                <div class="appointment-slot" data-time="15:30"></div>
                                <div class="appointment-slot" data-time="16:00"></div>
                                <div class="appointment-slot" data-time="16:30"></div>
                                <div class="appointment-slot" data-time="17:00"></div>
                                <div class="appointment-slot" data-time="17:30"></div>
                                <div class="appointment-slot" data-time="18:00"></div>
                            </div>

                            <!-- Cabelos Column -->
                            <div class="service-column" data-service="cabelos">
                                <div class="service-column-header">Cabelos</div>
                                <div class="appointment-slot" data-time="08:00"></div>
                                <div class="appointment-slot" data-time="08:30"></div>
                                <div class="appointment-slot" data-time="09:00"></div>
                                <div class="appointment-slot" data-time="09:30"></div>
                                <div class="appointment-slot" data-time="10:00"></div>
                                <div class="appointment-slot" data-time="10:30"></div>
                                <div class="appointment-slot" data-time="11:00"></div>
                                <div class="appointment-slot" data-time="11:30"></div>
                                <div class="appointment-slot" data-time="12:00"></div>
                                <div class="appointment-slot" data-time="12:30"></div>
                                <div class="appointment-slot" data-time="13:00"></div>
                                <div class="appointment-slot" data-time="13:30"></div>
                                <div class="appointment-slot" data-time="14:00"></div>
                                <div class="appointment-slot" data-time="14:30"></div>
                                <div class="appointment-slot" data-time="15:00"></div>
                                <div class="appointment-slot" data-time="15:30"></div>
                                <div class="appointment-slot" data-time="16:00"></div>
                                <div class="appointment-slot" data-time="16:30"></div>
                                <div class="appointment-slot" data-time="17:00"></div>
                                <div class="appointment-slot" data-time="17:30"></div>
                                <div class="appointment-slot" data-time="18:00"></div>
                            </div>

                            <!-- Noivas Column -->
                            <div class="service-column" data-service="noivas">
                                <div class="service-column-header">Noivas</div>
                                <div class="appointment-slot" data-time="08:00"></div>
                                <div class="appointment-slot" data-time="08:30"></div>
                                <div class="appointment-slot" data-time="09:00"></div>
                                <div class="appointment-slot" data-time="09:30"></div>
                                <div class="appointment-slot" data-time="10:00"></div>
                                <div class="appointment-slot" data-time="10:30"></div>
                                <div class="appointment-slot" data-time="11:00"></div>
                                <div class="appointment-slot" data-time="11:30"></div>
                                <div class="appointment-slot" data-time="12:00"></div>
                                <div class="appointment-slot" data-time="12:30"></div>
                                <div class="appointment-slot" data-time="13:00"></div>
                                <div class="appointment-slot" data-time="13:30"></div>
                                <div class="appointment-slot" data-time="14:00"></div>
                                <div class="appointment-slot" data-time="14:30"></div>
                                <div class="appointment-slot" data-time="15:00"></div>
                                <div class="appointment-slot" data-time="15:30"></div>
                                <div class="appointment-slot" data-time="16:00"></div>
                                <div class="appointment-slot" data-time="16:30"></div>
                                <div class="appointment-slot" data-time="17:00"></div>
                                <div class="appointment-slot" data-time="17:30"></div>
                                <div class="appointment-slot" data-time="18:00"></div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal for Adding/Editing Appointments -->
    <div class="modal" id="appointmentModal">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title" id="modalTitle">Novo Agendamento</h3>
                <span class="close-btn" id="closeModal">&times;</span>
            </div>
            <form id="appointmentForm">
                <input type="hidden" id="appointmentId">
                <div class="form-group">
                    <label for="clientName">Nome do Cliente:</label>
                    <input type="text" id="clientName" required>
                </div>
                <div class="form-group">
                    <label for="clientPhone">Telefone:</label>
                    <input type="tel" id="clientPhone" placeholder="(11) 99999-9999">
                </div>
                <div class="form-group">
                    <label for="serviceType">Serviço:</label>
                    <select id="serviceType" required>
                        <option value="maquiagem">Maquiagem</option>
                        <option value="unhas">Unhas</option>
                        <option value="cabelos">Cabelos</option>
                        <option value="noivas">Noivas</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="appointmentTime">Horário:</label>
                    <select id="appointmentTime" required>
                        <option value="08:00">08:00</option>
                        <option value="08:30">08:30</option>
                        <option value="09:00">09:00</option>
                        <option value="09:30">09:30</option>
                        <option value="10:00">10:00</option>
                        <option value="10:30">10:30</option>
                        <option value="11:00">11:00</option>
                        <option value="11:30">11:30</option>
                        <option value="12:00">12:00</option>
                        <option value="12:30">12:30</option>
                        <option value="13:00">13:00</option>
                        <option value="13:30">13:30</option>
                        <option value="14:00">14:00</option>
                        <option value="14:30">14:30</option>
                        <option value="15:00">15:00</option>
                        <option value="15:30">15:30</option>
                        <option value="16:00">16:00</option>
                        <option value="16:30">16:30</option>
                        <option value="17:00">17:00</option>
                        <option value="17:30">17:30</option>
                        <option value="18:00">18:00</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="status">Status:</label>
                    <select id="status" required>
                        <option value="confirmado">Confirmado</option>
                        <option value="pendente">Pendente</option>
                        <option value="cancelado">Cancelado</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="observations">Observações:</label>
                    <textarea id="observations" placeholder="Observações sobre o agendamento..."></textarea>
                </div>
                
                <div class="appointment-actions" id="appointmentActions" style="display: none;">
                    <button type="button" class="btn btn-success" id="confirmBtn">Confirmar</button>
                    <button type="button" class="btn btn-warning" id="cancelBtn">Cancelar</button>
                </div>
                
                <div class="modal-footer">
                    <button type="button" class="btn btn-danger" id="deleteBtn" style="display: none;">Excluir</button>
                    <button type="submit" class="btn btn-primary">Salvar</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        // Current date display
        function updateDate() {
            const now = new Date();
            const options = { 
                day: 'numeric', 
                month: 'long', 
                year: 'numeric', 
                weekday: 'long' 
            };
            document.getElementById('current-date').textContent = 
                now.toLocaleDateString('pt-BR', options);
        }
        updateDate();

        // Navigation
        document.querySelectorAll('.menu-item').forEach(item => {
            item.addEventListener('click', () => {
                // Remove active class from all menu items
                document.querySelectorAll('.menu-item').forEach(mi => mi.classList.remove('active'));
                // Add active class to clicked item
                item.classList.add('active');
                
                // Hide all page sections
                document.querySelectorAll('.page-section').forEach(section => section.classList.remove('active'));
                // Show selected page section
                const pageId = item.getAttribute('data-page');
                document.getElementById(pageId).classList.add('active');
            });
        });

        // Appointments data
        let appointments = [
            { 
                id: 1, 
                name: "Maria Silva", 
                phone: "(11) 99999-1111",
                service: "maquiagem", 
                time: "09:00", 
                status: "confirmado",
                observations: "Cliente preferencial, gosta de maquiagem natural"
            },
            { 
                id: 2, 
                name: "Joana Santos", 
                phone: "(11) 99999-2222",
                service: "unhas", 
                time: "11:00", 
                status: "pendente",
                observations: "Primeira vez no salão"
            }
        ];

        // DOM Elements
        const modal = document.getElementById('appointmentModal');
        const closeModal = document.getElementById('closeModal');
        const appointmentForm = document.getElementById('appointmentForm');
        const modalTitle = document.getElementById('modalTitle');
        const appointmentIdInput = document.getElementById('appointmentId');
        const clientNameInput = document.getElementById('clientName');
        const clientPhoneInput = document.getElementById('clientPhone');
        const serviceTypeInput = document.getElementById('serviceType');
        const appointmentTimeInput = document.getElementById('appointmentTime');
        const statusInput = document.getElementById('status');
        const observationsInput = document.getElementById('observations');
        const deleteBtn = document.getElementById('deleteBtn');
        const appointmentActions = document.getElementById('appointmentActions');
        const confirmBtn = document.getElementById('confirmBtn');
        const cancelBtn = document.getElementById('cancelBtn');
        const appointmentSlots = document.querySelectorAll('.appointment-slot');

        // Event Listeners
        closeModal.addEventListener('click', () => {
            modal.style.display = 'none';
        });

        window.addEventListener('click', (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
            }
        });

        // Open modal for new appointment
        appointmentSlots.forEach(slot => {
            slot.addEventListener('click', (e) => {
                // Ignore clicks on existing appointments
                if (e.target.closest('.appointment')) return;
                
                const time = slot.getAttribute('data-time');
                const service = slot.parentElement.getAttribute('data-service');
                
                modalTitle.textContent = 'Novo Agendamento';
                appointmentForm.reset();
                appointmentIdInput.value = '';
                serviceTypeInput.value = service;
                appointmentTimeInput.value = time;
                deleteBtn.style.display = 'none';
                appointmentActions.style.display = 'none';
                
                modal.style.display = 'flex';
            });
        });

        // Open modal for editing existing appointment
        function addAppointmentClickListeners() {
            document.querySelectorAll('.appointment').forEach(app => {
                app.addEventListener('click', (e) => {
                    e.stopPropagation();
                    const id = app.getAttribute('data-id');
                    const appointment = appointments.find(a => a.id == id);
                    
                    if (appointment) {
                        modalTitle.textContent = 'Editar Agendamento';
                        appointmentIdInput.value = appointment.id;
                        clientNameInput.value = appointment.name;
                        clientPhoneInput.value = appointment.phone || '';
                        serviceTypeInput.value = appointment.service;
                        appointmentTimeInput.value = appointment.time;
                        statusInput.value = appointment.status;
                        observationsInput.value = appointment.observations || '';
                        deleteBtn.style.display = 'block';
                        
                        // Show action buttons only for pending appointments
                        if (appointment.status === 'pendente') {
                            appointmentActions.style.display = 'flex';
                        } else {
                            appointmentActions.style.display = 'none';
                        }
                        
                        modal.style.display = 'flex';
                    }
                });
            });
        }

        // Confirm appointment
        confirmBtn.addEventListener('click', () => {
            const id = appointmentIdInput.value;
            if (id) {
                const index = appointments.findIndex(a => a.id == id);
                if (index !== -1) {
                    appointments[index].status = 'confirmado';
                    statusInput.value = 'confirmado';
                    refreshAppointments();
                }
            }
        });

        // Cancel appointment
        cancelBtn.addEventListener('click', () => {
            const id = appointmentIdInput.value;
            if (id) {
                const index = appointments.findIndex(a => a.id == id);
                if (index !== -1) {
                    appointments[index].status = 'cancelado';
                    statusInput.value = 'cancelado';
                    refreshAppointments();
                }
            }
        });

        // Form submission
        appointmentForm.addEventListener('submit', (e) => {
            e.preventDefault();
            
            const id = appointmentIdInput.value;
            const name = clientNameInput.value;
            const phone = clientPhoneInput.value;
            const service = serviceTypeInput.value;
            const time = appointmentTimeInput.value;
            const status = statusInput.value;
            const observations = observationsInput.value;
            
            if (id) {
                // Update existing appointment
                const index = appointments.findIndex(a => a.id == id);
                if (index !== -1) {
                    appointments[index] = { 
                        id: parseInt(id), 
                        name, 
                        phone, 
                        service, 
                        time, 
                        status, 
                        observations 
                    };
                }
            } else {
                // Create new appointment
                const newId = Math.max(0, ...appointments.map(a => a.id)) + 1;
                appointments.push({ 
                    id: newId, 
                    name, 
                    phone, 
                    service, 
                    time, 
                    status, 
                    observations 
                });
            }
            
            refreshAppointments();
            modal.style.display = 'none';
        });

        // Delete appointment
        deleteBtn.addEventListener('click', () => {
            const id = appointmentIdInput.value;
            if (id && confirm('Tem certeza que deseja excluir este agendamento?')) {
                appointments = appointments.filter(a => a.id != id);
                refreshAppointments();
                modal.style.display = 'none';
            }
        });

        // Refresh appointments display
        function refreshAppointments() {
            // Clear all appointments
            document.querySelectorAll('.appointment').forEach(app => {
                app.remove();
            });
            
            // Render appointments
            appointments.forEach(app => {
                const slot = document.querySelector(`.service-column[data-service="${app.service}"] .appointment-slot[data-time="${app.time}"]`);
                
                if (slot) {
                    const appointmentEl = document.createElement('div');
                    appointmentEl.className = `appointment ${app.status}`;
                    appointmentEl.setAttribute('data-id', app.id);
                    
                    appointmentEl.innerHTML = `
                        <div class="appointment-name">${app.name}</div>
                        <div class="appointment-status">${app.status}</div>
                    `;
                    
                    slot.appendChild(appointmentEl);
                }
            });
            
            // Update service counts
            const services = ['maquiagem', 'unhas', 'cabelos', 'noivas'];
            services.forEach(service => {
                const count = appointments.filter(a => a.service === service && a.status !== 'cancelado').length;
                const countEl = document.getElementById(`${service}-count`);
                if (countEl) {
                    countEl.textContent = count;
                }
            });
            
            // Re-add click listeners
            addAppointmentClickListeners();
        }

        // Initialize charts
        function initCharts() {
            // Services Chart
            const servicesCtx = document.getElementById('servicesChart').getContext('2d');
            new Chart(servicesCtx, {
                type: 'doughnut',
                data: {
                    labels: ['Maquiagem', 'Unhas', 'Cabelos', 'Noivas'],
                    datasets: [{
                        data: [25, 30, 35, 10],
                        backgroundColor: [
                            '#c39086',
                            '#d4a59a',
                            '#e2bdb5',
                            '#f4e4e1'
                        ],
                        borderWidth: 0
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'bottom'
                        }
                    }
                }
            });

            // Revenue Chart
            const revenueCtx = document.getElementById('revenueChart').getContext('2d');
            new Chart(revenueCtx, {
                type: 'line',
                data: {
                    labels: ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'],
                    datasets: [{
                        label: 'Receita',
                        data: [320, 450, 380, 520, 680, 890, 420],
                        borderColor: '#c39086',
                        backgroundColor: 'rgba(195, 144, 134, 0.1)',
                        tension: 0.4,
                        fill: true
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            display: false
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            ticks: {
                                callback: function(value) {
                                    return 'R$ ' + value;
                                }
                            }
                        }
                    }
                }
            });

            // Instagram Chart
            const instagramCtx = document.getElementById('instagramChart').getContext('2d');
            new Chart(instagramCtx, {
                type: 'bar',
                data: {
                    labels: ['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'],
                    datasets: [
                        {
                            label: 'Curtidas',
                            data: [45, 52, 38, 67, 89, 125, 78],
                            backgroundColor: 'rgba(195, 144, 134, 0.8)'
                        },
                        {
                            label: 'Comentários',
                            data: [8, 12, 6, 15, 18, 25, 14],
                            backgroundColor: 'rgba(212, 165, 154, 0.8)'
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'bottom'
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true
                        }
                    }
                }
            });

            // Social Comparison Chart
            const socialCtx = document.getElementById('socialComparisonChart').getContext('2d');
            new Chart(socialCtx, {
                type: 'line',
                data: {
                    labels: ['Semana 1', 'Semana 2', 'Semana 3', 'Semana 4'],
                    datasets: [
                        {
                            label: 'Instagram',
                            data: [450, 520, 480, 680],
                            borderColor: '#c39086',
                            backgroundColor: 'rgba(195, 144, 134, 0.1)',
                            tension: 0.4
                        },
                        {
                            label: 'Facebook',
                            data: [280, 320, 290, 380],
                            borderColor: '#d4a59a',
                            backgroundColor: 'rgba(212, 165, 154, 0.1)',
                            tension: 0.4
                        },
                        {
                            label: 'TikTok',
                            data: [150, 180, 220, 280],
                            borderColor: '#e2bdb5',
                            backgroundColor: 'rgba(226, 189, 181, 0.1)',
                            tension: 0.4
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'bottom'
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true
                        }
                    }
                }
            });
        }

        // Initialize
        refreshAppointments();
        
        // Initialize charts when dashboard is visible
        setTimeout(() => {
            initCharts();
        }, 100);
    </script>
</body>
</html>
